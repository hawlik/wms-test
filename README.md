# WMS Map Tester

A single-page tool for testing WMS (Web Map Service) layers on an interactive Leaflet map.

Paste a WMS URL, configure layer name, format, and other parameters, then preview the result directly on the map. Useful for quickly validating WMS endpoints during development.
